# data only container pattern
data:
  image: muccg/debian8-base:latest
  volumes:
    - data/dev:/data

createrepo:
  build: .
  #command: initrepos
  #command: recoverrepos
  #command: updaterepo ccg 6 x86_64
  command: updaterepos
  #command: uploadrepo ccg
  environment:
    - SYNC_DELETE=1
    - SYNC_FORCE=1
  volumes_from:
    - data
